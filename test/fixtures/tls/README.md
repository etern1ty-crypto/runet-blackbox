# Public test fixtures

This deliberately public private key belongs only to local test servers. It is not a credential and must never be deployed. Certificates cover example.com. The normal certificate is valid 2020–2040; the expired fixture ended in 2021. No real service certificate or key was used.
